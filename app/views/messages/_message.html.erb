<div class="panel">
	<div class="panel-body">
		<%= render 'shared/avatar', user: message.user, style: "img-circle avatar-small" %>
		<strong><%= message.user.fullname %></strong>

		<span class="pull-right"><%= message.message_time %></span>
		<br>
		<div class="row-space-2">
			<%= message.content %>
		</div>
	</div>
</div>